type Query {
    historicoPorPaciente(pacienteId: ID!): [HistoricoConsulta]
    historicoPorId(id: ID!): HistoricoConsulta
}

type HistoricoConsulta {
    id: ID
    idConsultaAgendada: ID
    pacienteId: ID
    medicoId: ID
    dataRealizacao: String
    especialidade: String
}